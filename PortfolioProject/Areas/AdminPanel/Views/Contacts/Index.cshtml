@model IEnumerable<PortfolioProject.Models.Contact>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/AdminPanel/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.contact_keyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contact_main_phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contact_secod_phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contact_email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.contact_keyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contact_main_phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contact_secod_phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contact_email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
